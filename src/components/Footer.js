import React from 'react';

const Footer = () => {
    return (
        <footer>
            <h1>My footer</h1>
            <h2>(Still don't know what to add to a footer)</h2>
        </footer>
    )
}

export default Footer